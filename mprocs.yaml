procs:
  flux-web:
    shell: "cd flux-web && bun dev"
    autoRestart: true

  flux-desktop:
    shell: "cd flux-desktop && bun dev"
    autoRestart: true

  # flux-extension:
  #   shell: "cd flux-extension && bun dev"
  #   autoRestart: true